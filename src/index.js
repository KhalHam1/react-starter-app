import ReactDOM from 'react-dom/client';
//This is the file that is executed initially when the program is run (Always).

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);   